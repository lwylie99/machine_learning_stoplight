import arcade

class GameConstants:
    # Define the window size
    WINDOW_WIDTH = 800
    WINDOW_HEIGHT = 600

    # Define the car size
    CAR_WIDTH = 30
    CAR_HEIGHT = 60

    # Define the intersection size
    INTERSECT_WIDTH = 200
    INTERSECTION_HEIGHT = 200

    # Define the car speed
    CAR_SPEED = 5

    # Define the traffic light timing
    GREEN_TIME = 5000
    YELLOW_TIME = 1000
    RED_TIME = 5000

    # Define the colors
    BLACK = arcade.color.BLACK
    WHITE = arcade.color.WHITE
    RED = arcade.color.RED
    GREEN = arcade.color.GREEN
    
    
    